import styled from 'styled-components'

export const Container = styled.button`
    display: flex;
    align-items: center;
    justify-content: space-around;


    width: min(100%, 10rem);
    height: 3rem;
    padding: .75rem;

    background: transparent;
    border: none;
    outline: none;
    cursor: pointer;

    margin: 2.375rem auto 7.125rem  auto;
`